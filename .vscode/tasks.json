{
    "tasks": [
        {
            "type": "shell",
            "label": "makebuild",
            "command": "gcc",
            "args": [
                "-g",
                "-O0",
                "-o",
                "main",
                "${file}"
            ],
            "options": {
                "cwd": "${fileDirname}"
            }
        },
        {
            "label": "build",
            "dependsOn": ["makebuild"],
            "dependsOrder": "sequence"
        }
    ],
    "version": "2.0.0"
}